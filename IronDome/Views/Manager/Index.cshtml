@model IEnumerable<IronDome.Models.DefenceAmmunition>

@{
    ViewData["Title"] = "Index";
}

<h1>DEfence Ammiunition</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.amount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <div class="yossi-miller">
                <a href="">this is from yossi miller</a>
                <p>come fake data from pachshish team/yossi branch</p>
            </div>
            <td>
                @Html.DisplayFor(modelItem => item.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
               @*  @Html.DisplayFor(modelItem => item.amount) *@
                    <form asp-action="updateDefenceAmmiunition" method="get">
                    <input name="amount" value="@item.amount" />
                    <input name="dfid" hidden="hidden" value="@item.id" />
                    <input type="submit" value="Update" />
                </form>
            </td>
        </tr>
}
    </tbody>
</table>
