@model IronDome.ViewModel.ThreatView

@{
    ViewData["Title"] = "createThreat";
}

<h1>Generate New Threat</h1>

<hr />

<div class="row">
    <div class="col-md-4">
        <form asp-action="createThreat">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @* <div class="form-group">
                <label asp-for="responseTime" class="control-label"></label>
                <input asp-for="responseTime" class="form-control" />
                <span asp-validation-for="responseTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="type.id" class="control-label"></label>
                <input asp-for="type.id" class="form-control" />
                <select>
                @*     @foreach ()
                    {
                        
                    } *@
                </select>
            </div> *@
            <div class="form-group">
                <label asp-for="fired_at" class="control-label"></label>
                <input asp-for="fired_at" class="form-control" />
                <span asp-validation-for="fired_at" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
